node_modules
dist
coverage
playwright-report
test-results
# The end-to-end suite's signed-in browser state: a live session cookie.
packages/app/e2e/.auth
*.tsbuildinfo

# Local secrets. `.env.example` is committed and is the contract.
.env
.env.*
!.env.example

.DS_Store
.vscode/*
!.vscode/extensions.json

# Local SQLite database created by `pnpm data migrate:local`.
local.db
local.db-*
